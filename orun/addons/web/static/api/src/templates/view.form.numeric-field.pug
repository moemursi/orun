extends view.form.field.pug

append vars
    -var cssClass = 'DecimalField'

block readonly
    | {{ (record.#{ name }|number:2) || '#{ field.emptyText }' }}

block field
    input.form-field.form-control(decimal)&attributes(attrs)
