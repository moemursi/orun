(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var Band, ChildBand, DataBand, PageFooter, PageHeader, ReportTitle, Runtime, base,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  base = require('./base');

  Runtime = require('./runtime');

  Band = (function(superClass) {
    extend(Band, superClass);

    Band.getClassName = function() {
      return 'Band';
    };

    function Band(parent) {
      Band.__super__.constructor.call(this, parent);
      this.canShrink = false;
      this.canBreak = false;
      this.childrenBands = [];
      this.startNewPage = false;
      this._fixed = false;
      this._staticBand = false;
      this._childBand = false;
    }

    Band.prototype.render = function(page, caller) {
      var band, child, h, i, j, k, l, len, len1, len2, len3, ny, obj, oy, ref, ref1, ref2, ref3;
      if (this.name) {
        page.document.engine.scope[this.name] = this;
      }
      if (this.childrenBands) {
        ref = this.childrenBands;
        for (i = 0, len = ref.length; i < len; i++) {
          band = ref[i];
          if (band._printBefore && band.parentNotification) {
            page = band.parentNotification(this, page);
          }
        }
      }
      h = 0;
      ref1 = this.children;
      for (j = 0, len1 = ref1.length; j < len1; j++) {
        child = ref1[j];
        obj = child.render(page);
        if (this._fixed) {
          page.addObject(child._obj);
        }
        h = Math.max(h, obj.bottom);
      }
      h = Math.max(h, this.height);
      if (this._fixed === false) {
        ny = 0;
        if ((h + page._y) > page._maxHeight) {
          oy = page._y;
          page = page.newPage(page.document, caller);
          ny = page._y;
        }
        ref2 = this.children;
        for (k = 0, len2 = ref2.length; k < len2; k++) {
          child = ref2[k];
          if (ny > 0) {
            child._obj.top -= oy;
            child._obj.top += ny;
          }
          page.addObject(child._obj);
          delete child._obj;
        }
      }
      page._y += h;
      if (this.childrenBands) {
        ref3 = this.childrenBands;
        for (l = 0, len3 = ref3.length; l < len3; l++) {
          band = ref3[l];
          if (!band._printBefore && band.parentNotification) {
            page = band.parentNotification(this, page);
          }
        }
      }
      return page;
    };

    return Band;

  })(base.BaseObject);

  DataBand = (function(superClass) {
    extend(DataBand, superClass);

    DataBand.getClassName = function() {
      return 'DataBand';
    };

    function DataBand(parent) {
      DataBand.__super__.constructor.call(this, parent);
      this.maxRows = 0;
      this.rowCount = 1;
      this.data = null;
      this.keepTogether = false;
      this._y = 0;
    }

    DataBand.prototype._calcHeight = function() {
      return this.height;
    };

    DataBand.prototype.render = function(page) {
      var c, i, j, k, len, len1, ref, ref1, results, results1, row, rows, total;
      if (this.data) {
        rows = this.data;
      } else if (this.rowCount) {
        rows = (function() {
          results = [];
          for (var i = 1, ref = this.rowCount; 1 <= ref ? i <= ref : i >= ref; 1 <= ref ? i++ : i--){ results.push(i); }
          return results;
        }).apply(this);
      }
      c = 0;
      results1 = [];
      for (j = 0, len = rows.length; j < len; j++) {
        row = rows[j];
        c++;
        page.document.engine.scope['row'] = c;
        ref1 = page.document.report.totals;
        for (k = 0, len1 = ref1.length; k < len1; k++) {
          total = ref1[k];
          if (total.band === this) {
            total.compute(this, page);
          }
        }
        page = DataBand.__super__.render.call(this, page, true);
        if (this.maxRows && c >= this.maxRows) {
          break;
        } else {
          results1.push(void 0);
        }
      }
      return results1;
    };

    return DataBand;

  })(Band);

  ChildBand = (function(superClass) {
    extend(ChildBand, superClass);

    ChildBand.getClassName(function() {
      return 'Child';
    });

    function ChildBand(parent) {
      ChildBand.__super__.constructor.call(this, parent);
      this.parentBand = null;
      this._childBand = true;
      this._printBefore = false;
    }

    return ChildBand;

  })(Band);

  ReportTitle = (function(superClass) {
    extend(ReportTitle, superClass);

    ReportTitle.getClassName = function() {
      return 'ReportTitle';
    };

    function ReportTitle(parent) {
      ReportTitle.__super__.constructor.call(this, parent);
      this._fixed = true;
      this._staticBand = true;
    }

    return ReportTitle;

  })(Band);

  PageHeader = (function(superClass) {
    extend(PageHeader, superClass);

    PageHeader.getClassName(function() {
      return 'PageHeader';
    });

    function PageHeader(parent) {
      PageHeader.__super__.constructor.call(this, parent);
      this._fixed = true;
      this._staticBand = true;
    }

    return PageHeader;

  })(Band);

  PageFooter = (function(superClass) {
    extend(PageFooter, superClass);

    PageFooter.getClassName = function() {
      return 'PageFooter';
    };

    function PageFooter(parent) {
      PageFooter.__super__.constructor.call(this, parent);
      this._fixed = true;
      this._staticBand = true;
    }

    PageFooter.prototype.render = function(page) {
      var oy;
      oy = page._y;
      page._y = page._maxHeight - this.height;
      PageFooter.__super__.render.call(this, page);
      page._maxHeight -= this.height;
      return page._y = oy;
    };

    return PageFooter;

  })(Band);

  Runtime.registerComponent(ChildBand);

  Runtime.registerComponent(DataBand);

  Runtime.registerComponent(ReportTitle);

  Runtime.registerComponent(PageHeader);

  Runtime.registerComponent(PageFooter);

  module.exports = {
    Band: Band,
    DataBand: DataBand,
    ChildBand: ChildBand,
    PageHeader: PageHeader,
    PageFooter: PageFooter
  };

}).call(this);



},{"./base":2,"./runtime":9}],2:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var BaseObject, BaseView, Runtime,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  Runtime = require('./runtime');

  BaseObject = (function() {
    function BaseObject(parent) {
      this.name = null;
      this.parent = parent;
      this.children = [];
      this.exportable = true;
      this.printable = true;
      this.height = 0;
      this.report = parent.report;
    }

    BaseObject.prototype.addObject = function(obj) {
      this.children.push(obj);
      return obj.parent = this;
    };

    BaseObject.prototype.clearCache = function() {
      if (this._cachedData) {
        return delete this._cachedData;
      }
    };

    BaseObject.prototype._build = function(page) {
      var PreparedObject, obj;
      PreparedObject = require('./engine').PreparedObject;
      obj = new PreparedObject(page, this.constructor.getClassName());
      obj.target = this;
      obj.height = this.height;
      return obj;
    };

    BaseObject.prototype.findObject = function(name) {
      var child, i, len, ref, s;
      ref = this.children;
      for (i = 0, len = ref.length; i < len; i++) {
        child = ref[i];
        if (child.name === name) {
          return child;
        }
        s = child.findObject(name);
        if (s) {
          return s;
        }
      }
    };

    BaseObject.prototype.load = function(obj) {
      var child, cls, i, k, len, ref, results, v;
      for (k in obj) {
        v = obj[k];
        if (k !== 'children') {
          this[k] = v;
        }
      }
      if (obj.children) {
        ref = obj.children;
        results = [];
        for (i = 0, len = ref.length; i < len; i++) {
          child = ref[i];
          cls = Runtime.getComponent(child.type);
          obj = new cls(this);
          obj.load(child);
          results.push(this.addObject(obj));
        }
        return results;
      }
    };

    BaseObject.prototype.render = function(page) {
      var child, i, len, ref;
      this._obj = this._build(page);
      ref = this.children;
      for (i = 0, len = ref.length; i < len; i++) {
        child = ref[i];
        child.render(page);
      }
      return this._obj;
    };

    BaseObject.prototype.toHtml = function(obj) {
      return '';
    };

    return BaseObject;

  })();

  BaseView = (function(superClass) {
    extend(BaseView, superClass);

    function BaseView(parent) {
      BaseView.__super__.constructor.call(this, parent);
      this.left = 0;
      this.top = 0;
      this.width = 0;
      this.shiftMode = 'Always';
      this.visible = true;
    }

    BaseView.prototype._build = function(page) {
      var obj;
      obj = BaseView.__super__._build.call(this, page);
      obj.width = this.width;
      obj.top = this.top + page._y;
      obj.bottom = this.top + this.height;
      obj.left = this.left + page._x;
      return obj;
    };

    BaseView.prototype.load = function(obj) {
      return BaseView.__super__.load.call(this, obj);
    };

    return BaseView;

  })(BaseObject);

  module.exports = {
    BaseObject: BaseObject,
    BaseView: BaseView
  };

}).call(this);



},{"./engine":3,"./runtime":9}],3:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var Document, Engine, PreparedObject, PreparedPage, Runtime, base,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  base = require('./base');

  Runtime = require('./runtime');

  Engine = (function() {
    Engine.canvas = null;

    function Engine() {
      this.pageCount = 0;
      this.pageNumber = 0;
      this.scope = Runtime.scope;
      this.expCache = {};
      this.totals = {};
    }

    Engine.prototype.bootstrap = function() {
      if (!Engine.canvas) {
        return Engine.canvas = 1;
      }
    };

    Engine.prototype.run = function(doc) {
      var i, j, len, len1, p, ref, ref1, report;
      report = doc.report;
      ref = report.pages;
      for (i = 0, len = ref.length; i < len; i++) {
        p = ref[i];
        p.render(doc);
      }
      ref1 = report.pages;
      for (j = 0, len1 = ref1.length; j < len1; j++) {
        p = ref1[j];
        p.clearCache();
      }
      return console.log('Page count:', doc.pages.length);
    };

    Engine.prototype.compileTemplate = function(s) {
      return Runtime.interpolate(s);
    };

    return Engine;

  })();

  Document = (function() {
    function Document(report1) {
      this.report = report1;
      this.pages = [];
      this.pageCount = 0;
      this.engine = new Engine();
      this.description = this.report.description;
      this.author = this.report.author;
      this.code = this.report.code;
    }

    Document.prototype.addPage = function(page) {
      this.pages.push(page);
      return this.pageCount = page.pageNumber = this.pages.length;
    };

    Document.prototype.save = function() {
      return {};
    };

    return Document;

  })();

  PreparedObject = (function() {
    function PreparedObject(parent1, type) {
      this.parent = parent1;
      if (type) {
        this.type = type;
      }
      this.content = null;
      this.left = 0;
      this.top = 0;
      this.width = 0;
      this.height = 0;
      this._x = 0;
      this._y = 0;
    }

    PreparedObject.prototype.load = function(obj) {
      var k, results, v;
      results = [];
      for (k in obj) {
        v = obj[k];
        results.push(this[k] = v);
      }
      return results;
    };

    return PreparedObject;

  })();

  PreparedPage = (function(superClass) {
    extend(PreparedPage, superClass);

    function PreparedPage(parent, page) {
      PreparedPage.__super__.constructor.call(this, parent);
      this.landscape = false;
      this.children = [];
      this.document = parent;
      this.index = 0;
      this.pageNumber = 0;
      if (page) {
        this.page = page;
        this.width = this.page.width;
        this.height = this.page.height;
      }
      this._y = page.marginTop;
      this._x = page.marginLeft;
      this.calcPrintableArea();
    }

    PreparedPage.prototype.addObject = function(obj) {
      return this.children.push(obj);
    };

    PreparedPage.prototype.calcPrintableArea = function() {
      this._maxHeight = this.page.height - this.page.marginTop - this.page.marginBottom;
      return this._maxWidth = this.page.width - this.page.marginLeft - this.page.marginRight;
    };

    PreparedPage.prototype.newPage = function(document, caller) {
      return this.page.newPage(document, caller);
    };

    return PreparedPage;

  })(PreparedObject);

  module.exports = {
    Engine: Engine,
    PreparedObject: PreparedObject,
    PreparedPage: PreparedPage,
    Document: Document
  };

}).call(this);



},{"./base":2,"./runtime":9}],4:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var ChildBand, GroupFooter, GroupHeader, Runtime,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  ChildBand = require('./band').ChildBand;

  GroupHeader = (function(superClass) {
    extend(GroupHeader, superClass);

    GroupHeader.getClassName = function() {
      return 'GroupHeader';
    };

    function GroupHeader(parent) {
      GroupHeader.__super__.constructor.call(this, parent);
      this._linkedFooter = null;
      this._printBefore = true;
    }

    GroupHeader.prototype.parentNotification = function(band, page) {
      var engine, v;
      engine = page.document.engine;
      v = engine.scope.$eval(this.expression);
      if (this._curData !== v) {
        page = this.render(page);
        this._curData = v;
      }
      return page;
    };

    return GroupHeader;

  })(ChildBand);

  GroupFooter = (function(superClass) {
    extend(GroupFooter, superClass);

    function GroupFooter() {
      return GroupFooter.__super__.constructor.apply(this, arguments);
    }

    GroupFooter.getClassName = function() {
      return 'GroupFooter';
    };

    GroupFooter.prototype.parentNotification = function(band, page) {};

    return GroupFooter;

  })(ChildBand);

  Runtime = require('./runtime');

  Runtime.registerComponent(GroupHeader);

  Runtime.registerComponent(GroupFooter);

}).call(this);



},{"./band":1,"./runtime":9}],5:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  var HtmlExport;

  HtmlExport = (function() {
    function HtmlExport(document) {
      this.document = document;
      this._y = 0;
      this._x = 0;
    }

    HtmlExport.prototype.toHtml = function() {
      var i, len, page, ref, s;
      s = '';
      ref = this.document.pages;
      for (i = 0, len = ref.length; i < len; i++) {
        page = ref[i];
        s += this.renderPage(page);
      }
      return s;
    };

    HtmlExport.prototype.renderObject = function(obj) {
      return obj.target.toHtml(obj);
    };

    HtmlExport.prototype.renderPage = function(page) {
      var i, len, obj, ref, s;
      s = '<div class="ng-report-print-page" style="';
      s += 'height:' + page.height.toString() + 'px;width:' + page.width.toString() + 'px;';
      s += '">';
      this._y += page.height;
      ref = page.children;
      for (i = 0, len = ref.length; i < len; i++) {
        obj = ref[i];
        s += this.renderObject(obj);
      }
      s += '</div>';
      return s;
    };

    return HtmlExport;

  })();

  module.exports = HtmlExport;

}).call(this);



},{}],6:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var Report, Runtime, _bootstrap, loadReport, ngReport, printReport, showReport, viewer;

  Runtime = require('./runtime');

  Report = require('./report').Report;

  viewer = require('./viewer');

  ngReport = angular.module('ngReport', []);

  _bootstrap = function(el) {
    if (!el) {
      el = angular.element(Runtime.reportElement);
    }
    angular.bootstrap(el, ['ngReport']);
    el.injector().invoke(function($interpolate) {
      return Runtime.interpolate = $interpolate;
    });
    return Runtime.scope = el.scope();
  };

  loadReport = function(el, file) {
    var doc, rep;
    el = _bootstrap(el);
    rep = new Report(el);
    rep.load(file);
    doc = rep.prepare();
    return doc;
  };

  showReport = function(el, rep, viewerType) {
    var doc, v;
    doc = loadReport(el, rep);
    if (viewerType === 'pdf') {
      v = new viewer.PDFViewer(doc);
    } else {
      v = new viewer.HtmlViewer(doc, el);
    }
    return v.show();
  };

  printReport = function(el, rep, format) {
    var doc;
    if (format == null) {
      format = 'html';
    }
    doc = loadReport(el, rep);
    return doc.report.print(format);
  };

  document.loadReport = loadReport;

  window.showReport = showReport;

  window.printReport = printReport;

}).call(this);



},{"./report":8,"./runtime":9,"./viewer":12}],7:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var Page, PaperSize, PreparedPage, base,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  base = require('./base');

  PaperSize = require('./report').PaperSize;

  Page = (function(superClass) {
    extend(Page, superClass);

    function Page(parent) {
      Page.__super__.constructor.call(this, parent);
      this.report = parent;
      this.marginBottom = 0;
      this.marginLeft = 0;
      this.marginTop = 0;
      this.marginRight = 0;
      this.landscape = false;
      this.width = PaperSize.A4.x;
      this.height = PaperSize.A4.y;
      this._staticBands = null;
      this._dataBands = null;
    }

    Page.prototype._build = function(document) {
      return new PreparedPage(document, this);
    };

    Page.prototype.clearCache = function() {
      var child, i, len, ref, results;
      Page.__super__.clearCache.call(this);
      ref = this.children;
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        child = ref[i];
        if (child.clearCache) {
          results.push(child.clearCache());
        } else {
          results.push(void 0);
        }
      }
      return results;
    };

    Page.prototype.load = function(obj) {
      var band, child, childrenBands, i, j, len, len1, oldBand, ref, results;
      Page.__super__.load.call(this, obj);
      childrenBands = [];
      ref = this.children;
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        band = ref[i];
        if (band._childBand) {
          if (band._printBefore) {
            childrenBands.push(band);
          } else {
            oldBand.childrenBands.push(band);
          }
          continue;
        } else if (childrenBands.length) {
          for (j = 0, len1 = childrenBands.length; j < len1; j++) {
            child = childrenBands[j];
            child.parentBand = band;
            band.childrenBands.push(child);
          }
          childrenBands = [];
        }
        results.push(oldBand = band);
      }
      return results;
    };

    Page.prototype.newPage = function(document, node) {
      var band, i, j, k, len, len1, len2, page, ref, ref1, ref2, results;
      if (!this._staticBands) {
        this._staticBands = [];
        this._dataBands = [];
        ref = this.children;
        for (i = 0, len = ref.length; i < len; i++) {
          band = ref[i];
          if (band._staticBand) {
            this._staticBands.push(band);
          } else if (!band._childBand) {
            this._dataBands.push(band);
          }
        }
      }
      page = this._build(document);
      document.addPage(page);
      this._page = page;
      ref1 = this._staticBands;
      for (j = 0, len1 = ref1.length; j < len1; j++) {
        band = ref1[j];
        band.render(page);
      }
      if (node) {
        return page;
      } else {
        ref2 = this._dataBands;
        results = [];
        for (k = 0, len2 = ref2.length; k < len2; k++) {
          band = ref2[k];
          if (!band._childBand) {
            results.push(band.render(page));
          } else {
            results.push(void 0);
          }
        }
        return results;
      }
    };

    Page.prototype.render = function(document) {
      return this.newPage(document);
    };

    return Page;

  })(base.BaseObject);

  module.exports = Page;

  PreparedPage = require('./engine').PreparedPage;

}).call(this);



},{"./base":2,"./engine":3,"./report":8}],8:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var Document, Page, PaperSize, Report, Runtime, Units, mm;

  Runtime = require('./runtime');

  Units = (function() {
    function Units() {}

    Units.mm = 3.779527559055;

    Units.cm = 37.79527559055;

    Units.inch = 96;

    return Units;

  })();

  mm = Units.mm;

  PaperSize = (function() {
    function PaperSize() {}

    PaperSize.A3 = {
      x: 297 * mm,
      y: 420 * mm
    };

    PaperSize.A4 = {
      x: 210 * mm,
      y: 297 * mm
    };

    PaperSize.A5 = {
      x: 148 * mm,
      y: 210 * mm
    };

    PaperSize.Custom = null;

    return PaperSize;

  })();

  Report = (function() {
    function Report(element) {
      this.element = element;
      this._curPage = null;
      this._report = null;
      this.units = 'mm';
      this.paperSize = 'A4';
      this.pageWidth = PaperSize.A4.x;
      this.pageHeight = PaperSize.A4.y;
      this.prepared = false;
      this.document = null;
      this.loaded = false;
      this.pages = [];
      this.totals = [];
    }

    Report.prototype.addPage = function(page) {
      return this.pages.push(page);
    };

    Report.prototype.findObject = function(name) {
      var i, len, obj, ref, s;
      ref = this.pages;
      for (i = 0, len = ref.length; i < len; i++) {
        obj = ref[i];
        if (obj.name === name) {
          return obj;
        }
        s = obj.findObject(name);
        if (s) {
          return s;
        }
      }
    };

    Report.prototype.load = function(report) {
      var Total, i, j, len, len1, p, page, ref, ref1, results, t, total;
      this._report = report;
      ref = report.pages;
      for (i = 0, len = ref.length; i < len; i++) {
        p = ref[i];
        page = new Page(this);
        this.addPage(page);
        page.load(p);
      }
      if (report.totals) {
        Total = Runtime.getComponent('Total');
        ref1 = report.totals;
        results = [];
        for (j = 0, len1 = ref1.length; j < len1; j++) {
          total = ref1[j];
          t = new Total(this);
          t.load(total);
          results.push(this.totals.push(t));
        }
        return results;
      }
    };

    Report.prototype.prepare = function() {
      if (!this.prepared) {
        this.document = new Document(this);
        this.document.engine.run(this.document);
        this.prepared = true;
      }
      return this.document;
    };

    Report.prototype.print = function(format) {
      var HtmlExport, html, shtml, wnd;
      if (format == null) {
        format = 'html';
      }
      if (format === 'html') {
        HtmlExport = require('./html');
        html = new HtmlExport(this.prepare());
        wnd = window.open('', 'ngReportPrintWnd');
        shtml = html.toHtml();
        wnd.document.write('<html><head><link href="css/print.css" rel="stylesheet" type="text/css"></head><body><div>');
        wnd.document.write(shtml);
        wnd.document.write('</div></body></html>');
        wnd.document.close();
        return $(wnd.document).ready(function() {
          return setTimeout(function() {
            wnd.focus();
            wnd.print();
            return wnd.close();
          }, 1);
        });
      }
    };

    return Report;

  })();

  module.exports = {
    Report: Report,
    PaperSize: PaperSize,
    Units: Units
  };

  Document = require('./engine').Document;

  Page = require('./page');

  require('./group');

  require('./base');

  require('./band');

  require('./text');

}).call(this);



},{"./band":1,"./base":2,"./engine":3,"./group":4,"./html":5,"./page":7,"./runtime":9,"./text":10}],9:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var Runtime;

  Runtime = (function() {
    function Runtime() {}

    Runtime.components = {};

    Runtime.getComponent = function(name) {
      return this.components[name];
    };

    Runtime.registerComponent = function(comp) {
      return this.components[comp.getClassName()] = comp;
    };

    Runtime.registerFormat = function(format) {
      return this.formats[format.getClassName()] = format;
    };

    return Runtime;

  })();

  module.exports = Runtime;

  require('./report');

  require('./group');

  require('./text');

  require('./total');

}).call(this);



},{"./group":4,"./report":8,"./text":10,"./total":11}],10:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var Runtime, TextObject, base,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  base = require('./base');

  Runtime = require('./runtime');

  TextObject = (function(superClass) {
    extend(TextObject, superClass);

    TextObject.getClassName = function() {
      return 'Text';
    };

    function TextObject(parent) {
      TextObject.__super__.constructor.call(this, parent);
      this.allowExpressions = true;
      this.autoWidth = false;
      this.canGrow = false;
      this.highlight = '';
      this.highlightStyle = {};
      this.format = '';
      this.htmlTags = false;
      this.textAlign = 'Left';
      this.verticalAlign = 'Top';
      this.lineHeight = 0;
      this.nullValue = '';
      this.text = '';
      this.wordWrap = true;
      this.border = {
        top: false,
        right: false,
        bottom: false,
        left: false,
        color: '#000',
        width: 1
      };
      this.font = {
        name: 'Arial',
        size: 10,
        bold: false,
        italic: false
      };
    }

    TextObject.prototype.load = function(obj) {
      TextObject.__super__.load.call(this, obj);
      return this.text = obj.text;
    };

    TextObject.prototype._build = function(page) {
      var obj;
      obj = TextObject.__super__._build.call(this, page);
      if (this.allowExpressions && this.text) {
        if (this._cachedData === void 0) {
          this._cachedData = page.document.engine.compileTemplate(this.text, this);
        }
        obj.content = this._cachedData(page.document.engine.scope);
      } else {
        obj.content = this.text;
      }
      obj.style = {
        'vertical-align': this.verticalAlign.toLowerCase(),
        'text-align': this.textAlign.toLowerCase(),
        'font-family': this.font.name,
        'font-size': this.font.size + 'pt'
      };
      if (this.lineHeight) {
        obj.style['line-height'] = this.lineHeight.toString() + 'px';
      }
      if (this.border.top) {
        obj.style['border-top'] = this.border.width.toString() + 'px solid ' + this.border.color + ';';
      }
      if (this.border.right) {
        obj.style['border-right'] = this.border.width.toString() + 'px solid ' + this.border.color + ';';
      }
      if (this.border.bottom) {
        obj.style['border-bottom'] = this.border.width.toString() + 'px solid ' + this.border.color + ';';
      }
      if (this.border.left) {
        obj.style['border-left'] = this.border.width.toString() + 'px solid ' + this.border.color + ';';
      }
      return obj;
    };

    TextObject.prototype.toHtml = function(obj) {
      var k, ref, s, style, v;
      s = '<div style="';
      style = 'position:absolute;overflow:hidden;display:inline;';
      style += 'left:' + obj.left.toString() + 'px;';
      style += 'top:' + obj.top.toString() + 'px;';
      if (obj.width) {
        style += 'max-width:' + obj.width.toString() + 'px;';
      }
      if (obj.height) {
        style += 'max-height:' + obj.height.toString() + 'px;';
      }
      if (obj.style) {
        ref = obj.style;
        for (k in ref) {
          v = ref[k];
          style += k + ':' + v.toString() + ';';
        }
      }
      s += style + '">' + obj.content + '</div>';
      return s;
    };

    return TextObject;

  })(base.BaseView);

  Runtime.registerComponent(TextObject);

}).call(this);



},{"./base":2,"./runtime":9}],11:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var AVG, COUNT, MAX, MIN, Runtime, SUM, Total,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  Runtime = require('./runtime');

  SUM = 'SUM';

  COUNT = 'COUNT';

  MAX = 'MAX';

  MIN = 'MIN';

  AVG = 'AVG';

  Total = (function() {
    Total.getClassName = function() {
      return 'Total';
    };

    function Total(report) {
      this.report = report;
      this.band = bind(this.band, this);
      this.expression = null;
      this.band = null;
      this.func = SUM;
      this.name = null;
    }

    Total.prototype.band = function(band) {
      return band;
    };

    Total.prototype.compute = function(band, page) {
      var curVal, engine, v;
      engine = page.document.engine;
      v = engine.scope.$eval(this.expression) || 0;
      curVal = engine.totals[this.name];
      if (this.func === COUNT) {
        if (curVal == null) {
          curVal = 0;
        }
        curVal += 1;
      } else if (this.func === SUM) {
        if (curVal == null) {
          curVal = v;
        } else {
          curVal += v;
        }
      } else if (this.func === AVG) {
        if (curVal === null) {
          curVal = v;
        } else {
          curVal = (this.currentValue + v) / 2;
        }
      } else if (this.func === MIN) {
        if (curVal === null) {
          curVal = v;
        } else {
          curVal = Math.min(this.currentValue, v);
        }
      } else if (this.func === MAX) {
        if (curVal === null) {
          curVal = v;
        } else {
          curVal = Math.max(this.currentValue, v);
        }
      }
      engine.totals[this.name] = curVal;
      engine.scope[this.name] = curVal;
      return curVal;
    };

    Total.prototype.load = function(obj) {
      this.func = obj.func;
      this.name = obj.name;
      this.expression = obj.expression;
      return this.band = this.report.findObject(obj.band);
    };

    Total.prototype.reset = function(band, page) {
      return page.document.engine.totals[this.name] = null;
    };

    return Total;

  })();

  Runtime.registerComponent(Total);

}).call(this);



},{"./runtime":9}],12:[function(require,module,exports){
// Generated by CoffeeScript 1.10.0
(function() {
  'use strict';
  var HtmlViewer, PDFViewer;

  HtmlViewer = (function() {
    function HtmlViewer(document, element) {
      this.document = document;
      this.element = element;
      this.pages = this.document.pages;
      this.pageIndex = -1;
    }

    HtmlViewer.prototype.clearView = function() {
      return this.container.html('');
    };

    HtmlViewer.prototype.show = function() {
      var report, s, viewer;
      s = this.renderViewer();
      $(this.element).html(s);
      this.container = $('#ng-report-preview-container');
      viewer = this;
      report = this.document.report;
      $('#btn-print').click(function() {
        return report.print();
      });
      $('#btn-first').click(function() {
        return viewer.first();
      });
      $('#btn-next').click(function() {
        return viewer.next();
      });
      $('#btn-prev').click(function() {
        return viewer.prev();
      });
      $('#btn-last').click(function() {
        return viewer.last();
      });
      return this.setPage(0);
    };

    HtmlViewer.prototype.setPage = function(idx) {
      this.pageIndex = idx;
      this.page = this.pages[idx];
      return this.renderPage();
    };

    HtmlViewer.prototype.first = function() {
      return this.setPage(0);
    };

    HtmlViewer.prototype.last = function() {
      return this.setPage(this.pages.length - 1);
    };

    HtmlViewer.prototype.next = function() {
      if ((this.pageIndex + 1) < this.pages.length) {
        return this.setPage(++this.pageIndex);
      }
    };

    HtmlViewer.prototype.prev = function() {
      if (this.pageIndex > 0) {
        return this.setPage(--this.pageIndex);
      }
    };

    HtmlViewer.prototype.renderViewer = function() {
      return '<div class="ng-report-preview-toolbar"><button id="btn-print" class="btn btn-default">Print</button> <button id="btn-first" class="btn btn-default"><< First</button><button id="btn-prev" class="btn btn-default">< Prev</button><button id="btn-next" class="btn btn-default">Next ></button><button id="btn-last" class="btn btn-default">Last >></button></div><div class="ng-report-preview-report"><div id="ng-report-preview-container"></div></div>';
    };

    HtmlViewer.prototype.renderObject = function(obj) {
      return obj.target.toHtml(obj);
    };

    HtmlViewer.prototype.renderPage = function() {
      var i, len, obj, ref, s;
      s = '<div class="ng-report-preview-page" style="';
      s += 'height:' + this.page.height.toString() + 'px;width:' + this.page.width.toString() + 'px;';
      s += '">';
      ref = this.page.children;
      for (i = 0, len = ref.length; i < len; i++) {
        obj = ref[i];
        s += this.renderObject(obj);
      }
      s += '</div>';
      this.container.html(s);
      return s;
    };

    return HtmlViewer;

  })();

  PDFViewer = (function() {
    function PDFViewer(element) {
      this.element = element;
    }

    PDFViewer.prototype.show = function() {};

    return PDFViewer;

  })();

  module.exports = {
    HtmlViewer: HtmlViewer,
    PDFViewer: PDFViewer
  };

}).call(this);



},{}]},{},[6]);
